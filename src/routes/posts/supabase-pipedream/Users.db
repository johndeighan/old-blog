create table if not exists Users (
	did         integer,
	FirstName   varchar(64),
	MiddleName  varchar(64),
	LastName    varchar(64),
	Suffix      varchar(16),
	DateOfBirth date,
	DateCreated date,
	constraint NameDOB primary key(FirstName, MiddleName, LastName, DateOfBirth)
	);
